import { LoadingindicatorDirective } from './loadingindicator.directive';

describe('LoadingindicatorDirective', () => {
  it('should create an instance', () => {
    const directive = new LoadingindicatorDirective();
    expect(directive).toBeTruthy();
  });
});
