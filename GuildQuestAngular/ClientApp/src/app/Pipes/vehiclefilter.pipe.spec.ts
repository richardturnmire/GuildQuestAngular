import { VehiclefilterPipe } from './vehiclefilter.pipe';

describe('VehiclefilterPipe', () => {
  it('create an instance', () => {
    const pipe = new VehiclefilterPipe();
    expect(pipe).toBeTruthy();
  });
});
